"use client";

const TasksPage = () => {
  return (
    <div className={`tasks-tab-con px-4 transition-all duration-300`}>
      {/* Header */}
      Task Page
    </div>
  );
};

export default TasksPage;
